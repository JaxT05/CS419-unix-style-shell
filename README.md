## General

This is a basic Unix shell script written in C that can interpret a singular pipe command.

## Compiling and Running
Ensure that your bash terminal/shell is opened inside of the project folder, and run `gcc shell.c` to compile the program. This will create a compiled `a.out` output file. 

Run `./a.out` to run the output file. 
